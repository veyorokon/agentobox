// Agentobox: allow unsigned extensions (ESR only)
lockPref("xpinstall.signatures.required", false);
// Don't auto-disable sideloaded extensions (managed environment)
lockPref("extensions.autoDisableScopes", 0);
lockPref("extensions.enabledScopes", 15);

// Enable Marionette for theme hot-reload (relay.py swaps CSS at runtime)
lockPref("marionette.enabled", true);
lockPref("marionette.port", 2828);

// Register /tmp/abox-theme.css at startup so @import in config.css doesn't 404
// on first boot. nsIStyleSheetService registrations don't persist across restarts,
// so this is needed for the initial load.
try {
    let sss = Components.classes["@mozilla.org/content/style-sheet-service;1"]
        .getService(Components.interfaces.nsIStyleSheetService);
    let uri = Components.classes["@mozilla.org/network/io-service;1"]
        .getService(Components.interfaces.nsIIOService)
        .newURI("file:///tmp/abox-theme.css", null, null);
    if (!sss.sheetRegistered(uri, sss.USER_SHEET)) {
        sss.loadAndRegisterSheet(uri, sss.USER_SHEET);
    }
} catch(e) {
    // intentional: CSS file may not exist yet on first boot — theme bridge
    // will push it once the dashboard connects and sends theme tokens.
    Components.utils.reportError("abox: initial theme load skipped: " + e);
}
