{
  "name": "agent-manager-mcp",
  "version": "0.1.0",
  "description": "MCP server for managing AgentBox worker agents",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && node dist/main.js",
    "clean": "rm -rf dist",
    "build": "tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
