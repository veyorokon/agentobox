# Dev environment overrides.
# Used by: make tf-plan ENV=dev, make tf-apply ENV=dev

# Domain
DOMAIN=dev.agentobox.com

# EC2
INSTANCE_TYPE=t3.small
KEY_PAIR_NAME=agentobox-dev

# Database (RDS or local postgres on EC2)
POSTGRES_PASSWORD=CHANGE_ME_BEFORE_DEPLOY
